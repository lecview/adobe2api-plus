# ───────────────────────────────────────────────────────────────
# adobe2api-plus 环境变量模板
#
# 一键部署（Docker，零配置开箱即用）：
#   docker compose up -d --build
#   数据库、表结构、管理员账号、安全密钥全部自动完成（用内置默认值）。
#
# 生产环境建议创建 .env 覆盖下方的安全密钥与管理员密码。
# 本地开发（非 Docker）：复制为 .env.development，并填写 DATABASE_URL。
# ───────────────────────────────────────────────────────────────

# ── 安全密钥（可选，有内置默认值）─────────────────────────────
# 生产环境务必改成自己的随机值，否则所有默认部署共享同一密钥：
# SESSION_SECRET=$(openssl rand -hex 32)
# SESSION_SECRET=

# 凭据加密密钥（≥16 字节）：openssl rand -hex 16
# ENCRYPTION_KEY=

# ── 管理员初始账号（首次启动自动创建）─────────────────────────
# 默认 admin / admin；生产环境务必修改密码
ADMIN_BOOTSTRAP_USERNAME=admin
ADMIN_BOOTSTRAP_PASSWORD=admin

# ── 数据库（Docker 一键部署自动配置，无需填写）────────────────
# 本地开发（非 Docker）取消注释并填真实值：
# DATABASE_URL=mysql://user:password@127.0.0.1:3306/adobe
#
# Docker 部署数据库账号（不填则默认：adobe / adobe，库名 adobe）
# MYSQL_ROOT_PASSWORD=adobe
# MYSQL_USER=adobe
# MYSQL_PASSWORD=adobe
# MYSQL_DATABASE=adobe

# ── Roxy 浏览器（sherlockToken 自动铸造；留空则走手动输入 token）──
# 只需 API 地址 + API Key。workspaceId / dirId 由程序自动获取/创建，无需配置。
# 端口：Roxy 客户端【API -> API配置 -> 端口】里显示的端口（默认 50000）。
ROXYBROWSER_API_BASE=http://127.0.0.1:50000
ROXYBROWSER_API_TOKEN=
# Docker 部署时，容器内 puppeteer 需直连宿主机 Chrome CDP，填【宿主机局域网 IP】
# （如 192.168.50.80）。start.sh 会自动探测并注入，通常无需手动填；
# 只有 IP 探测失败时手动在此设置。不要用 host.docker.internal（会被网关拦成 500）。
# ROXYBROWSER_CDP_HOST=
